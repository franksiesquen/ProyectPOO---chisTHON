<Modulo1Screen>:
    MDScreen:

        MDBoxLayout:
            orientation: "vertical"
            spacing: dp(12.5)
            padding: dp(12.5)

            MDTopAppBar:
                md_bg_color: app.theme_cls.primary_color
                elevation: 1
                title: "Módulo 1: Introducción al Lenguaje de programación Python"
                right_action_items: [["dots-vertical", lambda x: app.callback()]]

            # Sección de texto y scroll
            MDScrollView:
                MDLabel:
                    id: texto_label
                    text: app.cargar_texto_modulo1()
                    markup: True  # Permitir formateo del texto
                    size_hint_y: None
                    height: self.texture_size[1]
                    text_size: self.width, None
                    halign: "justify"

            # Sección de botones
            MDBoxLayout:
                size_hint_y: None
                height: dp(50)
                spacing: dp(20)  # Espaciado entre botones
                padding: dp(10)  # Padding uniforme
                orientation: "horizontal"  # Alineación horizontal
                pos_hint: {"center_x": 0.5, "center_y": 0.1}  # Centrando botones

                MDRectangleFlatButton:
                    text: "Volver"
                    size_hint: None, None
                    size: dp(150), dp(40)
                    on_release: root.manager.current = "lecciones"

<Modulo2Screen>:
    MDScreen:

        MDBoxLayout:
            orientation: "vertical"
            spacing: dp(12.5)
            padding: dp(12.5)

            MDTopAppBar:
                md_bg_color: app.theme_cls.primary_color
                elevation: 1
                title: "Módulo 2: Primeros pasos programando"

            # Sección de texto y scroll
            MDScrollView:
                MDLabel:
                    id: texto_label
                    text: app.cargar_texto_modulo2()
                    markup: True  # Permitir formateo del texto
                    size_hint_y: None
                    height: self.texture_size[1]
                    text_size: self.width, None
                    halign: "justify"

            # Sección de botones
            MDBoxLayout:
                size_hint_y: None
                height: dp(50)
                spacing: dp(20)  # Espaciado entre botones
                padding: dp(10)  # Padding uniforme
                
                orientation: "horizontal"  # Alineación horizontal
                pos_hint: {"center_x": 0.5, "center_y": 0.1}  # Centrando botones

                MDRectangleFlatButton:
                    text: "Ir a la consola"
                    size_hint: None, None
                    size: dp(150), dp(40)
                    halign: "center"
                    on_release: app.go_to_console()

                MDRectangleFlatButton:
                    text: "Volver al Menú"
                    size_hint: None, None
                    size: dp(150), dp(40)
                    halign: "center"
                    on_release: root.manager.current = "home"

<Modulo3Screen>:
    MDScreen:

        MDBoxLayout:
            orientation: "vertical"
            spacing: dp(12.5)
            padding: dp(12.5)

            MDTopAppBar:
                md_bg_color: app.theme_cls.primary_color
                elevation: 1
                title: "Módulo 3: Literales"

            # Sección de texto y scroll
            MDScrollView:
                MDLabel:
                    id: texto_label
                    text: app.cargar_texto_modulo3()
                    markup: True  # Permitir formateo del texto
                    size_hint_y: None
                    height: self.texture_size[1]
                    text_size: self.width, None
                    halign: "justify"

            # Sección de botones
            MDBoxLayout:
                size_hint_y: None
                height: dp(50)
                spacing: dp(20)  # Espaciado entre botones
                padding: dp(10)  # Padding uniforme
                orientation: "horizontal"  # Alineación horizontal
                pos_hint: {"center_x": 0.5, "center_y": 0.1}  # Centrando botones

                MDRectangleFlatButton:
                    text: "Ir a la consola"
                    size_hint: None, None
                    size: dp(150), dp(40)
                    on_release: app.go_to_console()

                MDRectangleFlatButton:
                    text: "Volver al Menú"
                    size_hint: None, None
                    size: dp(150), dp(40)
                    on_release: root.manager.current = "home"